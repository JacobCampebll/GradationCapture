/* Digit classifier weights.
 *
 * PLACEHOLDER — no model has been generated into this file yet. Run:
 *
 *     python3 tools/train_digits.py
 *
 * which overwrites this file with MODEL_META and MODEL_WEIGHTS_B64. Until then
 * modelAvailable() in index.html returns false and the app stays in manual-entry
 * mode, which is fully working — recognition is the accelerator, not the app.
 *
 * This file exists rather than being absent so the page does not 404 on load.
 */
